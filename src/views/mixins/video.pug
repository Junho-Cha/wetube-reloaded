mixin video(video)
  .video-mixin 
    |
    |
    |
    .video-mixin__thumb
    .video-mixin__data
      span.video-mixin__title= video.title
      .video-mixin__meta
        span #{ video.meta.views }회
